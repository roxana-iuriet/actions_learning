name: 'Hello'
description: 'My first action'

inputs:
  input_file:
    description: 'input file'
    required: true
  output_file:
    description: 'out path'
    required: true

runs:
  using: 'docker'
  image: 'Dockerfile'
