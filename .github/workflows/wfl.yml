name: test action
on:
  push

env:
  IN_PATH: in/text.txt
  OUT_PATH: out/text2.txt

jobs:
  my_first_job:
    runs-on: ubuntu-latest
    name: A job to test
    steps:
      - name: create a file
        run: mkdir in && echo 'some test to write' > ${{env.IN_PATH}}
      - name: chckout code
        uses: actions/checkout@v2
      - name: test action
        id: hello
        uses: ./actions-a
        with:
          in_path: ${{env.IN_PATH}}
          out_path: ${{env.OUT_PATH}}